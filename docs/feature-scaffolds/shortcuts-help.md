# Keyboard shortcuts help (`?`)

**Status:** Live — `ShortcutsHelp` in root layout.

- `?` opens overlay (ignored inside inputs)
- Esc / click closes; Tab trapped while open
- Documents boot skip, Konami, gallery arrows
